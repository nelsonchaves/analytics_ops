#!/usr/bin/env bash
set -euo pipefail
IFS=$'\n\t'

cd "$(dirname "$0")/.."

printf 'Installing Analytics Ops dependencies...\n'
bundle install

printf '\nSetup complete. Try: bin/analytics-ops help\n'
